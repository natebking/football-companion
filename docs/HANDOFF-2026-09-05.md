# Handoff, 2026-09-05 (Saturday, college week 2)

Written by Claude at ~2:10 pm PT when its account spend limit hit. Codex picks up from here; Nathan tests live on his phone this afternoon and evening. Read `docs/CONTRACT.md` first (binding spec), then `web/app.js` (three scopes: SHELL, PRIME, LIVE).

## Where things stand

- Repo `natebking/football-companion` is PRIVATE on GitHub, `main`, last pushed commit `3df222e` (08-30). Live site: https://football-companion-rose.vercel.app (companion at `/`, v0 stopwatch at `/stopwatch`). Vercel project `football-companion`, deploy with `vercel deploy --prod --yes` from the repo root (a plain `vercel deploy` preview returns 302 behind deployment protection, so verify on prod).
- The app was complete and deployed on 08-30 but had never touched a live game. Today's work (uncommitted in the working tree unless the commit below landed): `web/app.js`, `web/index.html`, `docs/CONTRACT.md` (new section "Live feed repair"), `.github/workflows/rebuild-tendencies.yml` (header was a markdown blockquote, so every run failed at parse; now `#` comments, YAML validates).
- `CFBD_API_KEY` repository secret is SET (from `.env`, never printed). The Action has still never run; trigger `gh workflow run rebuild-tendencies.yml -f dry_run=true --repo natebking/football-companion` after pushing and read the run summary.
- Build stamp in the app: `VERSION = '2026-09-05b'`, shown as "build 2026-09-05b" at the bottom of the game sheet. If the phone shows no build line, it is on the old bundle: pull to refresh.
- Commits today: `3738841` (live path repair, diagnostics, Action YAML) and `e6b80e8` (finding #1 below, prints_number chip). Both pushed; prod verified by curl for the build stamp and in a browser against URI at Temple (no console errors, card, ask and feed all rendered).
- Action dry run was dispatched at 21:02 UTC as run 33991873877 (`gh run view 33991873877 --repo natebking/football-companion`); read its step summary before trusting the Tuesday cron.

## What changed in app.js today, and why (all measured on real ESPN JSON)

Harvested six complete games and two live snapshots from today into `/private/tmp/claude-502/-Users-nking-Documents-TheStratSuite/8d3d1c70-4584-4a10-88b9-b0ed504fea12/scratchpad/espn/` (final_*.json, live_*.json, sb_*.json). Replay harness at `.../scratchpad/sweep/replay.js` slices `collectPlays / classify / isMarker / spotFromText / fixSituation / sitKeyOf / preSnap / playSitKey` verbatim out of app.js and drives them prefix by prefix (`node replay.js [app.js path] [data dir]`).

| metric over 1,036 poll states | before | after |
|---|---|---|
| wrong next-situation | 50 | 21 (7 are the seconds before a half's clock hits 0:00, 3 are harness artefacts) |
| no card when one was due (last play a timeout/period marker) | 58 | 0 (20 nulls remain, all correct: after a TD, at halftime) |
| asks voided by a timeout before the snap | 5 | 0 |

Rules added (documented in CONTRACT.md "Live feed repair"): `isMarker` skips Timeout/End Period/End of Half/End of Game rows and they never emit `result`; `spotFromText` + `fixSituation` fix the flipped `yardsToEndzone` (21 of 917 end blocks, exact mirror, `possessionText` right on all 917), distance 0/negative on an end block = 1st and 10, new possession = 1st and 10 or and goal; `preSnap` returns null at `STATUS_HALFTIME` and at period 2/4 with clock 0:00; `sameSnap` grades a call when offense and down match and the ball is within 5 yards (ESPN moves the spot a yard or three on ~1 snap in 50). `classify` types fumbles by the play text; interception-return TDs render as a score. `MAX_DELAY` 60 → 90 s (streaming). Diagnostics ring (`fc_diag`, last 500 events: boot, game, poll, snap, nosnap, card, ask, grade, err) plus a "Copy diagnostics" button in the game sheet (clipboard, share-sheet fallback) so tonight can be debriefed: Nathan pastes the JSON to Claude/Codex.

## Findings not yet acted on, ranked

From Codex (sol/high, full JSON at `.../scratchpad/codex_review.json`) and a Claude workflow (5 lenses; only the first finding below survived verification before the limit hit; the rest are unverified, full text at `/private/tmp/claude-502/-Users-nking-Documents-TheStratSuite/8d3d1c70-4584-4a10-88b9-b0ed504fea12/tasks/w38lyi2vo.output`). Line numbers refer to the current working tree.

1. **DONE in `e6b80e8`. Cards with `prints_number:false` drew the team chip and the "they" register** (app.js ~642 `st.attributed`, ~750 chip). Eight cards (trail_late_pass, lead_late_run, two_minute_pass, d4_short_own_half, d4_field_goal, d4_kick_range_edge, d4_punt_returner, d1_short_rare) state a league-wide fact, yet the chip says "East Carolina · n=46" whenever the pass-rate cell happens to be attributable: 65 of 1,034 real snaps (6.3%). Fix: `st.attributed = !!(ten && ten.can_attribute && card.prints_number !== false);` and draw the team chip on `st.attributed`, not on `ten.can_attribute`. Every card already ships `tendency_low`. Do this first; it is a 2-line change.
2. **Codex HIGH, app.js ~1360: overlapping summary responses are not bound to the game that requested them.** `pollGame` on `visibilitychange` can start a second loop; a late response for game A can land after `selectGame(B)`. Fix: a `gen` counter incremented in `selectGame`/`resetGame`, captured per request, response dropped if stale; and guard `pollGame` so only one in-flight request exists.
3. **Codex HIGH, app.js ~503: a "sticks" ask grades an interception return by `statYardage`**, which is return yardage. Fix: in `grade`, if `r.kind === 'pass'` and the play was an interception (pass `turnover: true` from LIVE), void with "They threw it and the defense caught it, so that call does not count."
4. **Codex HIGH, app.js ~609: an answered ask that ESPN skips past is discarded silently on the next snap** and `nosnap` hides but does not clear pending. Fix: on `snap`/`nosnap` with an answered pending, log it as voided and show "That one got away from the feed, no grade." rather than nothing.
5. Codex MEDIUM: poll loop keeps running at 3 s after `post` (stop or back off to 30 s); wake-lock rejection is silent (show a dim "screen may sleep" note in the header); `STATUS_DELAYED` counts as live and leaves the card up (treat like halftime: null situation, message already exists in `waitingMessage`).

Unverified workflow titles worth a look, in order: "Live game posts two plays with the same start block and omits the 4th-down snap" (live_401858433.json, drives.previous[1].plays[5..6]; ESPN revised a play in place, so `collectPlays` in-place replacement by id is right but the card built from the first version may have primed a snap that never came); "Failed 4th-down conversion" end block; "Answered call is silently discarded" (same as #4); "Touchdown typed 'Pass Reception'" (a TD whose `type.text` is Pass Reception renders without the score colour); "Card top line shows ESPN's raw downDistanceText"; "classify() voids real rushes" (check the raw-text fallbacks against every distinct `type.text` in the six finals); "Game picker sheet prints live…" (copy nit).

## Remaining steps, in order

1. Read the Action dry-run summary (run 33991873877). If it failed, fix `engine/refresh.py` or the workflow and re-dispatch; the Tuesday cron depends on it.
2. Findings #2, #3, #4, each with a harness run (`node replay.js` numbers must not regress: 928 right, 21 wrong, 20 marker nulls), `node --check`, commit in the repo's voice, push, `vercel deploy --prod --yes`, bump `VERSION` to `2026-09-05c`, confirm with `curl -s https://football-companion-rose.vercel.app/app.js | grep -c 2026-09-05c`.
3. When Nathan pastes diagnostics JSON after a game: `events` is the ring; look for `err` rows, `poll` rows with `sit: null` while `status` is IN_PROGRESS (a missed card), `grade` rows with `ok: null` (voided calls) and the gap between `ask` and `grade` timestamps (feed latency vs his delay setting).

## Test checklist for Nathan's phone (send this to him)

1. Open https://football-companion-rose.vercel.app, pull to refresh, tap "Pick a game", confirm the sheet bottom says "build 2026-09-05b".
2. Pick the game on your TV. Leave TV delay at 0 for the first drive. If a card or feed row ever shows up before you see the play, tap "TV +0s" and raise it until they stop (cable is usually 20 to 45 s, streaming can be 60 to 90).
3. Watch three things: does the card's down and distance match the TV's graphic before the snap; does the "Call it" ask appear roughly every 6 to 8 plays and grade right after the play; does the card come down at halftime and come back with the kickoff.
4. At any point, and at the end: game sheet, "Copy diagnostics", paste into chat. That JSON is the whole debrief.
5. Things that are expected and not bugs: "Offenses throw here 68%" instead of the team name on about half the cards; no card for a few seconds after a touchdown until the kickoff; FCS visitors (Tennessee State, NDSU, SDSU) always get the league number.

## Preferences that bind Codex too

No em dashes anywhere. No exclamation points or hype in card copy. Push back by default. PST times. CONTRACT.md is the spec; if you deviate, say so in the commit message. Never print or commit `.env`. The v0 root `index.html` is frozen.
